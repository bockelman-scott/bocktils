const httpUtils = require( "@toolbocks/http" );

const logUtils = require( "@toolbocks/logging" );

const { SimpleLogger } = logUtils;


describe( "HTTP Modules tests", () =>
{

    test( "TBD", () =>
    {
        expect( 1 ).toEqual( 1 );

    }, 10_000 );
} );