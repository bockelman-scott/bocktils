{
  "name": "@toolbocks/json",
  "version": "1.0.0",
  "description": "Parse and Interpolate JSON (even if it contains circular-dependencies) and convert self-referential object to JSON",
  "main": "src/JsonUtils.cjs",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@toolbocks/core": "^1.0.0",
    "jest": "^29.7.0"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/bockelman-scott/bocktils.git"
  },
  "private": true
}
